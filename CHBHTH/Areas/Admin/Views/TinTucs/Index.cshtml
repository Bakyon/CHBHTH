@model IEnumerable<CHBHTH.Models.TinTuc>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>News</h2>

<p>
    @Html.ActionLink("Add news", "Create")
</p>

<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TieuDe)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TieuDe)
        </td>
        <td>
            <button class="btn btn-success">
                @Html.ActionLink("Edit", "Edit", new { id = item.MaTT }, new { @style = "color:white;" })
            </button>
            <button class="btn btn-primary">
                @Html.ActionLink("View", "Details", new { id = item.MaTT }, new { @style = "color:white;" })
            </button>
            <button class="btn btn-danger">
                @Html.ActionLink("Delete", "Delete", new { id = item.MaTT }, new { @style = "color:white;" })
            </button>
        </td>
    </tr>
}

</table>
